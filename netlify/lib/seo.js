'use strict';
function slugify(value){return String(value||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'');}
function escapeXml(value){return String(value??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&apos;');}
function productPath(product){return `/producto/${slugify(product.name)}-${product.id}`;}
function categoryPath(category){return `/categoria/${slugify(category)}`;}
function buildSitemap({baseUrl='https://nutretium.com',products=[],categories=[]}={}){const base=String(baseUrl).replace(/\/+$/,'');const urls=[{loc:`${base}/`,changefreq:'daily',priority:'1.0'}];for(const category of categories){if(!String(category||'').trim())continue;urls.push({loc:`${base}${categoryPath(category)}`,changefreq:'weekly',priority:'0.8'});}for(const product of products){if(!product||product.active===false||!Number.isFinite(Number(product.id)))continue;urls.push({loc:`${base}${productPath(product)}`,changefreq:'weekly',priority:product.featured?'0.8':'0.7'});}const unique=[],seen=new Set();for(const item of urls){if(seen.has(item.loc))continue;seen.add(item.loc);unique.push(item);}return `<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n${unique.map((u)=>`  <url>\n    <loc>${escapeXml(u.loc)}</loc>\n    <changefreq>${u.changefreq}</changefreq>\n    <priority>${u.priority}</priority>\n  </url>`).join('\n')}\n</urlset>\n`;}
module.exports={slugify,escapeXml,productPath,categoryPath,buildSitemap};
