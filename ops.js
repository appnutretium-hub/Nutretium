(function(){'use strict';const KEY='nutretium_user';const $=(id)=>document.getElementById(id);const esc=(v)=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function user(){try{return JSON.parse(localStorage.getItem(KEY)||'null')}catch{return null}}function token(){return(user()||{}).token||''}async function api(path){const r=await fetch(path,{headers:{Authorization:'Bearer '+token()}});const d=await r.json().catch(()=>({}));if(r.status===401||r.status===403)throw Object.assign(new Error(d.error||'Acceso no autorizado.'),{auth:true});if(!r.ok)throw new Error(d.error||'No se pudo cargar el panel.');return d}function metric(label,value){return `<div class="card"><small>${esc(label)}</small><strong>${esc(value)}</strong></div>`}async function load(){try{const[status,quality]=await Promise.all([api('/.netlify/functions/admin-status'),api('/.netlify/functions/catalog-quality')]);$('login').hidden=true;$('app').hidden=false;$('metrics').innerHTML=[metric('Productos activos',quality.stats.active),metric('Con foto',quality.stats.withImage),metric('Con descripción',quality.stats.withDescription),metric('Stock validado',quality.stats.knownStock),metric('Fichas incompletas',quality.stats.incomplete)].join('');$('services').innerHTML=Object.entries(status.services).map(([k,v])=>`<div class="service"><b class="${v?'ok':'bad'}">${v?'● Operativo':'● Pendiente'}</b><span>${esc(k)}</span></div>`).join('');const warnings=[...status.warnings];Object.entries(quality.issueCounts||{}).forEach(([k,v])=>warnings.push(`${v} productos: ${k}.`));$('warnings').innerHTML=warnings.length?warnings.map(w=>`<div class="warning">${esc(w)}</div>`).join(''):'<div class="service"><b class="ok">Sin alertas detectadas</b></div>';$('issues').innerHTML=(quality.issues||[]).map(i=>`<tr><td>${esc(i.name)}</td><td>${esc(i.code||'—')}</td><td>${esc(i.category||'—')}</td><td>${esc(i.missing.join(', '))}</td></tr>`).join('')||'<tr><td colspan="4" class="ok">No hay fichas incompletas según los campos auditados.</td></tr>';$('checked').textContent='Revisado: '+new Date(status.checkedAt).toLocaleString('es-ES')}catch(e){if(e.auth){$('app').hidden=true;$('login').hidden=false;$('loginError').textContent=e.message}else{alert(e.message)}}}$('loginForm').addEventListener('submit',async(e)=>{e.preventDefault();$('loginError').textContent='';try{const r=await fetch('/.netlify/functions/auth',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'login',email:$('email').value.trim(),password:$('password').value})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'No se pudo entrar.');localStorage.setItem(KEY,JSON.stringify(d.user));await load()}catch(err){$('loginError').textContent=err.message}});$('refresh').addEventListener('click',load);load()})();
