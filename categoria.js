'use strict';const products=(window.NUTRETIUM_PRODUCTS||[]).filter(p=>p.active!==false);const slug=v=>String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');const esc=v=>String(v||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const money=v=>new Intl.NumberFormat('es-ES',{style:'currency',currency:'EUR'}).format(v);const requested=decodeURIComponent(new URLSearchParams(location.search).get('slug')||location.pathname.split('/').filter(Boolean).pop()||'');const category=(window.NUTRETIUM_CATEGORIES||[]).find(c=>slug(c)===requested);const rows=products.filter(p=>p.category===category);document.getElementById('title').textContent=category||'Categoría no encontrada';document.getElementById('count').textContent=`${rows.length} productos disponibles`;document.title=`${category||'Categoría'} | Nutretium`;document.getElementById('grid').innerHTML=rows.map(p=>`<a class="card" href="/producto.html?slug=${encodeURIComponent(`${slug(p.name)}-${String(p.code).toLowerCase()}`)}">${p.image?`<img src="/${esc(String(p.image).replace(/^\//,''))}" alt="${esc(p.name)}">`:`<div class="placeholder">N</div>`}<h2>${esc(p.name)}</h2><b>${money(p.price)}</b></a>`).join('')||'<p class="not-found">No hay productos disponibles.</p>';
