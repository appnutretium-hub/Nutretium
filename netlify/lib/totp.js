'use strict';
const crypto=require('crypto');const ALPHABET='ABCDEFGHIJKLMNOPQRSTUVWXYZ234567';function decodeBase32(input){const clean=String(input||'').toUpperCase().replace(/[^A-Z2-7]/g,'');let bits='';for(const c of clean){const v=ALPHABET.indexOf(c);if(v<0)throw new Error('Secret TOTP no válido');bits+=v.toString(2).padStart(5,'0')}const bytes=[];for(let i=0;i+8<=bits.length;i+=8)bytes.push(parseInt(bits.slice(i,i+8),2));return Buffer.from(bytes)}function code(secret,time=Date.now(),step=30,digits=6){const counter=Math.floor(time/1000/step),buf=Buffer.alloc(8);buf.writeBigUInt64BE(BigInt(counter));const h=crypto.createHmac('sha1',decodeBase32(secret)).update(buf).digest(),offset=h[h.length-1]&15,value=((h[offset]&127)<<24)|(h[offset+1]<<16)|(h[offset+2]<<8)|h[offset+3];return String(value%(10**digits)).padStart(digits,'0')}function verify(secret,input,time=Date.now()){const given=String(input||'').replace(/\s/g,'');if(!/^\d{6}$/.test(given))return false;for(const drift of[-30000,0,30000]){const expected=code(secret,time+drift),a=Buffer.from(given),b=Buffer.from(expected);if(a.length===b.length&&crypto.timingSafeEqual(a,b))return true}return false}function secrets(){try{const parsed=JSON.parse(process.env.STAFF_TOTP_SECRETS||'{}');return parsed&&typeof parsed==='object'?parsed:{}}catch{return{}}}function secretFor(email){const map=secrets(),key=Object.keys(map).find(k=>k.toLowerCase()===String(email||'').toLowerCase());return key?String(map[key]||''):''}module.exports={decodeBase32,code,verify,secretFor};
