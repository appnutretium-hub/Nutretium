'use strict';
const crypto=require('crypto');const {requireStaff}=require('../lib/staff');const {getBlobStore}=require('../lib/blob-store');const enterprise=require('../lib/enterprise-store');const {cabecerasCORS}=require('../lib/cors');const CORS=cabecerasCORS('POST, OPTIONS'),URLS={test:'https://sis-t.redsys.es:25443/sis/rest/trataPeticionREST',production:'https://sis.redsys.es/sis/rest/trataPeticionREST'},SYSTEM={email:'system@nutretium.local',role:'system'};const resp=(s,b)=>({statusCode:s,headers:{...CORS,'Cache-Control':'no-store'},body:JSON.stringify(b)});
function signingKey(secret,order){const key=Buffer.from(secret,'base64'),iv=Buffer.alloc(8,0),cipher=crypto.createCipheriv('des-ede3-cbc',key,iv);cipher.setAutoPadding(false);const buf=Buffer.alloc(Math.ceil(order.length/8)*8,0);buf.write(order,'utf8');return Buffer.concat([cipher.update(buf),cipher.final()]);}function sign(params,secret,order){return crypto.createHmac('sha256',signingKey(secret,order)).update(params).digest('base64');}function decodeParameters(v){try{return JSON.parse(Buffer.from(v,'base64').toString('utf8'))}catch{return null}}
exports.handler=async event=>{if(event.httpMethod==='OPTIONS')return{statusCode:204,headers:CORS,body:''};if(event.httpMethod!=='POST')return resp(405,{error:'Method Not Allowed'});const auth=await requireStaff(event,'returns.manage');if(!auth.ok)return resp(auth.statusCode,{error:auth.error});let body;try{body=JSON.parse(event.body||'{}')}catch{return resp(400,{error:'JSON no válido.'})}const orderId=String(body.orderId||''),amountCents=Number(body.amountCents);if(!orderId||!Number.isInteger(amountCents)||amountCents<=0)return resp(400,{error:'Pedido e importe en céntimos son obligatorios.'});const secret=process.env.REDSYS_SECRET_KEY,merchant=process.env.REDSYS_MERCHANT_CODE,terminal=process.env.REDSYS_TERMINAL||'1',env=process.env.REDSYS_ENV||'test';if(!secret||!merchant)return resp(503,{error:'Redsys no está configurado.'});const orders=getBlobStore('redsys-orders');if(!orders)return resp(503,{error:'Pedidos no disponibles.'});const order=await orders.get(orderId,{type:'json',consistency:'strong'}).catch(()=>null);if(!order||order.status!=='PAID'||order.amountMismatch)return resp(409,{error:'El pedido no es reembolsable automáticamente.'});const paid=Math.round(Number(order.amount||0)*100),refunded=Number(order.refundedCents||0);if(amountCents>paid-refunded)return resp(409,{error:'El reembolso supera el saldo reembolsable.'});const mp={DS_MERCHANT_AMOUNT:String(amountCents),DS_MERCHANT_ORDER:orderId,DS_MERCHANT_MERCHANTCODE:merchant,DS_MERCHANT_CURRENCY:'978',DS_MERCHANT_TRANSACTIONTYPE:'3',DS_MERCHANT_TERMINAL:terminal},params=Buffer.from(JSON.stringify(mp)).toString('base64'),payload={Ds_SignatureVersion:'HMAC_SHA256_V1',Ds_MerchantParameters:params,Ds_Signature:sign(params,secret,orderId)};let r,data;try{r=await fetch(URLS[env]||URLS.test,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});data=await r.json().catch(()=>({}))}catch(err){return resp(502,{error:'No se pudo conectar con Redsys.',details:err.message})}const decoded=decodeParameters(data.Ds_MerchantParameters||data.DS_MERCHANTPARAMETERS)||data,code=String(decoded.Ds_Response||decoded.DS_RESPONSE||'');if(!r.ok||code!=='0900'){await enterprise.save('system-incidents',{title:`Reembolso Redsys fallido ${orderId}`,severity:'high',status:'open',orderId,amountCents,responseCode:code||null},SYSTEM,{reason:'refund-failure'}).catch(()=>{});return resp(502,{error:'Redsys no confirmó la devolución.',responseCode:code||null})}const nextRefunded=refunded+amountCents,refund={id:crypto.randomUUID(),at:new Date().toISOString(),amountCents,by:auth.email,responseCode:code,authorisationCode:decoded.Ds_AuthorisationCode||decoded.DS_AUTHORISATIONCODE||null};const updated={...order,refundedCents:nextRefunded,refunds:[...(order.refunds||[]),refund],refundStatus:nextRefunded===paid?'REFUNDED':'PARTIALLY_REFUNDED'};await orders.setJSON(orderId,updated);const invoiceId=`refund:${orderId}:${refund.id}`;await enterprise.save('invoices',{id:invoiceId,orderId,customerEmail:order.email||'',currency:'EUR',status:'issued',type:'credit_note',amountCents:-amountCents,issuedAt:new Date().toISOString(),reference:refund.id},auth,{id:invoiceId,reason:'redsys-refund'}).catch(()=>{});return resp(200,{ok:true,orderId,refundedCents:nextRefunded,remainingCents:paid-nextRefunded,refund});};
exports._test={signingKey,sign,decodeParameters};
