'use strict';
const {getBlobStore}=require('../lib/blob-store');const enterprise=require('../lib/enterprise-store');
exports.handler=async function(){try{const backup=getBlobStore('enterprise-backups-v1');if(!backup)throw new Error('Backup store unavailable');const snapshot=await enterprise.snapshot();const day=new Date().toISOString().slice(0,10);await backup.setJSON(`daily/${day}`,snapshot);const list=await backup.list({prefix:'daily/'});const old=(list.blobs||[]).sort((a,b)=>a.key.localeCompare(b.key)).slice(0,-30);await Promise.all(old.map(b=>backup.delete(b.key).catch(()=>{})));return{statusCode:200,body:JSON.stringify({ok:true,key:`daily/${day}`,domains:Object.keys(snapshot.domains).length})};}catch(err){console.error('[backup]',err);return{statusCode:500,body:JSON.stringify({ok:false,error:'Backup failed'})}}};
