'use strict';const assert=require('assert');delete process.env.REQUIRE_STAFF_MFA;delete process.env.REQUIRE_STAFF_COOKIE;process.env.CONTEXT='production';const s=require('../netlify/lib/security-policy');assert.equal(s.staffMfaRequired(),false);assert.equal(s.staffCookieRequired(),false);process.env.REQUIRE_STAFF_MFA='true';assert.equal(s.staffMfaRequired(),true);console.log('[test-simple-admin-login] production defaults to email/password; MFA remains opt-in')