'use strict';const assert=require('assert');const hooks=require('../netlify/lib/webhooks');const worker=require('../netlify/functions/notification-worker')._test;let n=0;function t(name,fn){fn();n++;console.log('✓',name)}t('firma webhook HMAC estable',()=>assert.strictEqual(hooks.signature('secret','body'),hooks.signature('secret','body')));t('firma cambia con payload',()=>assert.notStrictEqual(hooks.signature('secret','body'),hooks.signature('secret','body2')));t('plantillas sustituyen variables',()=>assert.strictEqual(worker.render('Hola {{name}}',{name:'Ana'}),'Hola Ana'));t('variable inexistente queda vacía',()=>assert.strictEqual(worker.render('{{missing}}',{}),''));console.log(`\n${n} pruebas de servicios superadas.`);
