'use strict';
const outbox=require('./outbox');
const webhooks=require('./webhooks');
const {sendEmail}=require('./email');
const {record}=require('./observability');
function orderEmail(payload){const order=String(payload.order||''),amount=Number(payload.amount||0).toFixed(2),mode=payload.fulfillment==='pickup'?'recogida en tienda':'envío';return{to:payload.email,subject:`Pedido ${order} confirmado — Nutretium`,html:`<div style="font-family:Arial,sans-serif"><h2>Pedido confirmado</h2><p>Hemos confirmado el pedido <strong>${order}</strong> por <strong>${amount} €</strong>.</p><p>Modalidad: <strong>${mode}</strong>.</p><p>Te avisaremos cuando avance la preparación.</p></div>`,idempotencyKey:`nutretium-enterprise-finalize/${order}`};}
async function execute(job){if(job.type==='order.customer_email'){if(!job.payload?.email)return{skipped:true,reason:'no-email'};return sendEmail(orderEmail(job.payload));}if(job.type==='webhook.order_paid')return webhooks.emit('order.paid',job.payload||{});if(job.type==='webhook.order_payment_failed')return webhooks.emit('order.payment_failed',job.payload||{});throw new Error(`Tipo de outbox no soportado: ${job.type}`);}
async function drain(limit=25){const summary={scanned:0,claimed:0,completed:0,retried:0,failed:0};const candidates=await outbox.due(limit);summary.scanned=candidates.length;for(const candidate of candidates){const claimed=await outbox.claim(candidate.path);if(!claimed)continue;summary.claimed++;try{await execute(claimed.job);if(await outbox.finish(claimed.path,claimed.claimId,{ok:true}))summary.completed++;}catch(err){await outbox.finish(claimed.path,claimed.claimId,{ok:false,error:err.message});const current=await outbox.getByPath(claimed.path).catch(()=>null);if(current?.status==='failed')summary.failed++;else summary.retried++;await record(current?.status==='failed'?'outbox_dead_letter':'outbox_failure',{severity:current?.status==='failed'?'high':'warning',source:'outbox-dispatch',message:String(err.message||err),tags:{type:claimed.job.type,id:claimed.job.id,traceId:String(claimed.job.payload?.traceId||'')}}).catch(()=>{});}}return summary;}
module.exports={orderEmail,execute,drain};
