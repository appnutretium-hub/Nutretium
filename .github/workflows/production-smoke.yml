name: Nutretium Production Smoke

on:
  workflow_run:
    workflows: ["Nutretium Full Quality Gate"]
    types: [completed]
    branches: [main]
  schedule:
    # Cada 6 horas. El smoke que de verdad importa ya lo dispara el workflow_run
    # de arriba tras cada Full Quality Gate correcto en main.
    - cron: "0 */6 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: production-smoke
  cancel-in-progress: true

jobs:
  smoke:
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 8
    env:
      NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
      NETLIFY_API_TOKEN: ${{ secrets.NETLIFY_API_TOKEN }}
      AUTO_ROLLBACK_ENABLED: ${{ secrets.AUTO_ROLLBACK_ENABLED }}
      REQUIRE_COMMERCE_LIVE_SMOKE: ${{ secrets.REQUIRE_COMMERCE_LIVE_SMOKE }}
    steps:
      - name: Verify public storefront
        shell: bash
        run: |
          set -euo pipefail
          base="https://nutretium.com"
          curl --fail --silent --show-error --location --retry 12 --retry-delay 10 --retry-all-errors --connect-timeout 10 --max-time 30 -D /tmp/headers -o /tmp/home "$base/"
          grep -qi '^x-frame-options: *DENY' /tmp/headers
          grep -qi '^x-content-type-options: *nosniff' /tmp/headers
          grep -qi '^strict-transport-security:' /tmp/headers
          grep -qi '^content-security-policy:' /tmp/headers
          grep -qi '^cross-origin-resource-policy: *same-site' /tmp/headers
          grep -qi 'Nutretium' /tmp/home

      - name: Verify exact revision is published
        id: revision
        shell: bash
        env:
          EXPECTED_SHA: ${{ github.event_name == 'workflow_run' && github.event.workflow_run.head_sha || github.sha }}
        run: |
          set -euo pipefail
          echo "deployed=false" >> "$GITHUB_OUTPUT"
          base="https://nutretium.com"
          for i in $(seq 1 18); do
            if curl --fail --silent --show-error --location --connect-timeout 10 --max-time 20 -o /tmp/build-meta "$base/build-meta.json?check=$i"; then
              if EXPECTED_SHA="$EXPECTED_SHA" node - <<'NODE'
          const fs=require('fs');
          const expected=String(process.env.EXPECTED_SHA||'').trim();
          const meta=JSON.parse(fs.readFileSync('/tmp/build-meta','utf8'));
          if(!/^[0-9a-f]{40}$/i.test(expected))throw new Error('EXPECTED_SHA inválido');
          if(meta.commitRef!==expected)process.exit(2);
          console.log('[production-smoke] revisión publicada confirmada:',meta.commitRef);
          NODE
              then
                echo "deployed=true" >> "$GITHUB_OUTPUT"
                exit 0
              fi
            fi
            sleep 10
          done
          echo "La revisión esperada $EXPECTED_SHA no está publicada; no se hará rollback de una versión sana." >&2
          exit 1

      - name: Verify admin access surface
        shell: bash
        run: |
          set -euo pipefail
          base="https://nutretium.com"
          curl --fail --silent --show-error --location --retry 6 --retry-delay 5 --retry-all-errors --connect-timeout 10 --max-time 30 -o /tmp/admin "$base/admin.html"
          grep -Fq 'id="formAcceso"' /tmp/admin
          grep -Fq '/staff-session-bridge.js' /tmp/admin
          grep -Fq '/staff-login-ui.js' /tmp/admin

          login_code="$(curl --silent --show-error --connect-timeout 10 --max-time 30 \
            -o /tmp/staff-login-probe -w '%{http_code}' \
            -X POST \
            -H 'Origin: https://nutretium.com' \
            -H 'Content-Type: application/json' \
            --data '{"email":"admin-smoke-invalid@nutretium.invalid","password":"invalid-smoke-password"}' \
            "$base/.netlify/functions/staff-login")"
          if [[ "$login_code" != "401" ]]; then
            echo "El endpoint staff-login no respondió con el rechazo seguro esperado (HTTP $login_code)." >&2
            cat /tmp/staff-login-probe >&2 || true
            exit 1
          fi
          node - <<'NODE'
          const fs=require('fs');
          const body=JSON.parse(fs.readFileSync('/tmp/staff-login-probe','utf8'));
          if(!/credenciales incorrectas/i.test(String(body.error||'')))throw new Error('staff-login no devolvió el contrato de rechazo esperado.');
          NODE

          session_code="$(curl --silent --show-error --connect-timeout 10 --max-time 30 \
            -o /tmp/admin-session-probe -w '%{http_code}' \
            -X POST \
            -H 'Origin: https://nutretium.com' \
            -H 'Content-Type: application/json' \
            -H 'X-Nutretium-Request: production-admin-smoke-0001' \
            --data '{"action":"status"}' \
            "$base/.netlify/functions/admin-session")"
          if [[ "$session_code" != "401" ]]; then
            echo "El endpoint admin-session no rechazó de forma segura una sesión ausente (HTTP $session_code)." >&2
            cat /tmp/admin-session-probe >&2 || true
            exit 1
          fi
          echo '[production-smoke] superficie admin operativa · bridge HttpOnly + login MFA + sesión interna responden'

      - name: Verify Guardian runtime is live
        shell: bash
        run: |
          set -euo pipefail
          base="https://nutretium.com"
          grep -qi 'guardian-runtime.js' /tmp/home
          curl --fail --silent --show-error --location --retry 6 --retry-delay 5 --retry-all-errors --connect-timeout 10 --max-time 30 -o /tmp/guardian-runtime "$base/guardian-runtime.js"
          grep -Fq 'NUTRETIUM Guardian Runtime' /tmp/guardian-runtime
          grep -Fq 'nt:guardian:success' /tmp/guardian-runtime
          grep -Fq '/.netlify/functions/client-error' /tmp/guardian-runtime

      - name: Verify responsible disclosure endpoint
        shell: bash
        run: |
          set -euo pipefail
          curl --fail --silent --show-error --location --retry 6 --retry-delay 5 --retry-all-errors --connect-timeout 10 --max-time 30 -o /tmp/security-txt "https://nutretium.com/.well-known/security.txt"
          grep -qi '^Contact: mailto:' /tmp/security-txt
          grep -qi '^Canonical: https://nutretium.com/.well-known/security.txt' /tmp/security-txt

      - name: Verify storefront health and report commerce readiness
        shell: bash
        run: |
          set -euo pipefail
          curl --fail --silent --show-error --location --retry 6 --retry-delay 10 --retry-all-errors --connect-timeout 10 --max-time 30 -o /tmp/status "https://nutretium.com/.netlify/functions/site-status"
          node - <<'NODE'
          const fs=require('fs');
          const body=JSON.parse(fs.readFileSync('/tmp/status','utf8'));
          if(body.maintenance!==false || body.storefrontReady!==true) throw new Error('El storefront público no está operativo.');
          if(body.commerceReady===true){
            console.log('[production-smoke] storefront operativo · comercio real preparado');
          }else{
            const blockers=Array.isArray(body.commerceBlockers)?body.commerceBlockers:['readiness-reason-unavailable'];
            console.log('[production-smoke] storefront operativo · comercio real aún no activado · bloqueos:',blockers.join(', '));
          }
          NODE

      - name: Require rollback controls when commerce is live
        shell: bash
        run: |
          set -euo pipefail
          commerce_ready="$(node -e "const fs=require('fs');const b=JSON.parse(fs.readFileSync('/tmp/status','utf8'));process.stdout.write(String(b.commerceReady===true))")"
          if [[ "$commerce_ready" == "true" ]]; then
            if [[ "$AUTO_ROLLBACK_ENABLED" != "true" || -z "$NETLIFY_SITE_ID" || -z "$NETLIFY_API_TOKEN" ]]; then
              echo "Comercio real está activo, pero rollback automático no está configurado." >&2
              exit 1
            fi
          fi

      - name: Require live commerce when explicitly enabled
        if: ${{ env.REQUIRE_COMMERCE_LIVE_SMOKE == 'true' }}
        shell: bash
        run: |
          set -euo pipefail
          node - <<'NODE'
          const fs=require('fs');
          const body=JSON.parse(fs.readFileSync('/tmp/status','utf8'));
          if(body.commerceReady!==true){
            const blockers=Array.isArray(body.commerceBlockers)?body.commerceBlockers:['readiness-reason-unavailable'];
            throw new Error(`El gate exige comercio real, pero sigue bloqueado por: ${blockers.join(', ')}`);
          }
          NODE

      - name: Guarded automatic rollback
        if: ${{ failure() && steps.revision.outputs.deployed == 'true' && env.AUTO_ROLLBACK_ENABLED == 'true' && env.NETLIFY_SITE_ID != '' && env.NETLIFY_API_TOKEN != '' }}
        shell: bash
        run: |
          set -euo pipefail
          echo "Production smoke failed on the published candidate; requesting Netlify rollback to previous deploy."
          curl --fail --silent --show-error -X PUT \
            -H "Authorization: Bearer $NETLIFY_API_TOKEN" \
            "https://api.netlify.com/api/v1/sites/$NETLIFY_SITE_ID/rollback"
          sleep 5
          curl --fail --silent --show-error --location --retry 12 --retry-delay 5 --retry-all-errors --connect-timeout 10 --max-time 30 https://nutretium.com/ >/tmp/rollback-home
          grep -qi 'Nutretium' /tmp/rollback-home

      - name: Rollback not configured warning
        if: ${{ failure() && steps.revision.outputs.deployed == 'true' && (env.AUTO_ROLLBACK_ENABLED != 'true' || env.NETLIFY_SITE_ID == '' || env.NETLIFY_API_TOKEN == '') }}
        run: echo "Production smoke failed on the published candidate; automatic rollback is not explicitly enabled/configured."

      - name: Unpublished revision safety notice
        if: ${{ failure() && steps.revision.outputs.deployed != 'true' }}
        run: echo "Expected main revision is not live. The previous published deploy is left untouched."
