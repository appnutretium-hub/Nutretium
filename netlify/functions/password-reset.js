'use strict';
const crypto=require('crypto');const {getBlobStore}=require('../lib/blob-store');const usuarios=require('../lib/usuarios');const {sendEmail}=require('../lib/email');const {cabecerasCORS}=require('../lib/cors');const CORS=cabecerasCORS('POST, OPTIONS'),response=(s,b)=>({statusCode:s,headers:{...CORS,'Cache-Control':'no-store'},body:JSON.stringify(b)});const EMAIL=/^[^\s@]+@[^\s@]+\.[^\s@]+$/;function digest(v){return crypto.createHash('sha256').update(String(v||'')).digest('hex')}function passwordHash(password){const salt=crypto.randomBytes(16).toString('hex'),hash=crypto.pbkdf2Sync(password,salt,100000,64,'sha512').toString('hex');return`${salt}:${hash}`}
exports.handler=async event=>{if(event.httpMethod==='OPTIONS')return{statusCode:204,headers:CORS,body:''};if(event.httpMethod!=='POST')return response(405,{error:'Method Not Allowed'});let body;try{body=JSON.parse(event.body||'{}')}catch{return response(400,{error:'JSON no válido.'})}const store=getBlobStore('password-reset-v1');if(!store)return response(503,{error:'Recuperación de contraseña no disponible.'});if(body.action==='request'){const email=String(body.email||'').trim().toLowerCase();if(!EMAIL.test(email))return response(200,{ok:true});const user=await usuarios.lee(email);if(!user)return response(200,{ok:true});if(!process.env.RESEND_API_KEY)return response(503,{error:'El correo de recuperación no está configurado.'});const raw=crypto.randomBytes(32).toString('base64url'),id=digest(raw),expiresAt=Date.now()+30*60*1000;await store.setJSON(id,{email,expiresAt,used:false,createdAt:new Date().toISOString()},{onlyIfNew:true});const base=String(process.env.PUBLIC_SITE_URL||'https://nutretium.com').replace(/\/$/,'');const link=`${base}/?reset=${encodeURIComponent(raw)}`;const ok=await sendEmail({to:email,subject:'Restablecer contraseña de Nutretium',html:`<p>Se ha solicitado cambiar la contraseña de tu cuenta Nutretium.</p><p><a href="${link}">Restablecer contraseña</a></p><p>El enlace caduca en 30 minutos y solo puede usarse una vez.</p>`});if(!ok){await store.delete(id).catch(()=>{});return response(503,{error:'No se pudo enviar el correo de recuperación.'})}return response(200,{ok:true})}if(body.action==='reset'){const raw=String(body.token||''),newPassword=String(body.newPassword||'');if(raw.length<30||newPassword.length<10||newPassword.length>200)return response(400,{error:'Token o contraseña no válidos.'});const id=digest(raw),record=await store.get(id,{type:'json',consistency:'strong'}).catch(()=>null);if(!record||record.used||Number(record.expiresAt)<Date.now())return response(400,{error:'El enlace ha caducado o ya fue utilizado.'});const user=await usuarios.lee(record.email);if(!user)return response(400,{error:'El enlace ya no es válido.'});await usuarios.escribe(record.email,{...user,passwordHash:passwordHash(newPassword),passwordChangedAt:new Date().toISOString(),sessionVersion:Number(user.sessionVersion||0)+1,updatedAt:new Date().toISOString()});await store.setJSON(id,{...record,used:true,usedAt:new Date().toISOString()});return response(200,{ok:true})}return response(400,{error:'Acción no reconocida.'})};exports._test={digest,passwordHash};
