# NUTRETIUM — plantilla de variables de entorno
# Los valores reales se configuran en Netlify → Site configuration → Environment variables.
# Nunca guardes secretos reales en el repositorio.

# ── Redsys ────────────────────────────────────────────────────────────────────
REDSYS_SECRET_KEY=
REDSYS_MERCHANT_CODE=
REDSYS_TERMINAL=1
REDSYS_ENV=test
URL_OK=
URL_KO=
MERCHANT_URL=
COMMERCE_LIVE=false

# ── Sesiones / Security Core Zero Trust ───────────────────────────────────────
JWT_SECRET=
# MFA ya no se fuerza globalmente: owner/admin están exentos y el resto del
# personal se activa individualmente desde Centro de Administración → Equipo.
# MFA_ENCRYPTION_KEY es opcional; si se omite se deriva una clave separada de JWT_SECRET.
MFA_ENCRYPTION_KEY=
# Compatibilidad heredada únicamente para secretos antiguos. Las nuevas altas
# MFA se guardan cifradas por usuario y no necesitan editar esta variable.
STAFF_TOTP_SECRETS=
# Cookie HttpOnly, CSRF y step-up continúan forzándose en producción.
REQUIRE_STAFF_COOKIE=false
REQUIRE_STAFF_CSRF=false
REQUIRE_STAFF_STEP_UP=false
GUEST_ORDER_SECRET=
MAINTENANCE_MODE=false

# ── Administración / GitHub ──────────────────────────────────────────────────
ADMIN_EMAILS=
OWNER_EMAILS=
STAFF_ROLES_JSON=
GITHUB_TOKEN=
GITHUB_REPO=appnutretium-hub/Nutretium
GITHUB_BRANCH=main

# ── Persistencia Netlify Blobs / vault ────────────────────────────────────────
SITE_ID=
NETLIFY_API_TOKEN=
CONFIG_VAULT_KEY=

# ── CORS ──────────────────────────────────────────────────────────────────────
ALLOWED_ORIGIN=

# ── Email / Resend ────────────────────────────────────────────────────────────
RESEND_API_KEY=
ORDER_NOTIFICATION_EMAIL=
ORDER_EMAIL_FROM=
CONTACT_EMAIL=appnutretium@gmail.com

# ── Envíos ────────────────────────────────────────────────────────────────────
SHIPPING_ENABLED=false
SHIPPING_COUNTRY=España
SHIPPING_RATE_CENTS=
SHIPPING_FREE_FROM_CENTS=
SHIPPING_LABEL=Envío
SHIPPING_PROVIDER_URL=
SHIPPING_PROVIDER_TOKEN=
SHIPPING_PROVIDER_SECRET=
SHIPPING_PROVIDER_WEBHOOK_SECRET=

# ── Promociones / fidelización ────────────────────────────────────────────────
NUTRETIUM_COUPONS_JSON=
NUTRETIUM_POINTS_ENABLED=false
NUTRETIUM_POINTS_PER_EURO=

# ── Integraciones opcionales Enterprise ────────────────────────────────────────
RECURRING_PROVIDER_URL=
RECURRING_PROVIDER_TOKEN=
RECURRING_PROVIDER_SECRET=
SUBSCRIPTION_WORKER_ENABLED=false
SMS_PROVIDER_URL=
SMS_PROVIDER_TOKEN=
SMS_PROVIDER_SECRET=
WHATSAPP_PROVIDER_URL=
WHATSAPP_PROVIDER_TOKEN=
WHATSAPP_PROVIDER_SECRET=
PUSH_PROVIDER_URL=
PUSH_PROVIDER_TOKEN=
PUSH_PROVIDER_SECRET=
CRON_SECRET=