/** NUTRETIUM — verified Redsys server callback. */
'use strict';
const crypto=require('crypto');const HEADERS={'Content-Type':'text/plain; charset=utf-8'};const {getBlobStore}=require('../lib/blob-store');const {sendEmail,buildOrderEmail}=require('../lib/email');const finalize=require('../lib/order-finalize');
async function getStore(){return getBlobStore('redsys-orders')}function deriveSigningKey(secretKeyBase64,orderNumber){const keyBuffer=Buffer.from(secretKeyBase64,'base64'),iv=Buffer.alloc(8,0),cipher=crypto.createCipheriv('des-ede3-cbc',keyBuffer,iv);cipher.setAutoPadding(false);const orderBuffer=Buffer.alloc(Math.ceil(orderNumber.length/8)*8,0);orderBuffer.write(orderNumber,'utf8');return Buffer.concat([cipher.update(orderBuffer),cipher.final()]);}function hmacBase64(dataBase64,signingKey){return crypto.createHmac('sha256',signingKey).update(dataBase64).digest('base64')}function toBase64Url(b64){return b64.replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/,'')}function safeEqual(a,b){const ba=Buffer.from(String(a)),bb=Buffer.from(String(b));return ba.length===bb.length&&crypto.timingSafeEqual(ba,bb)}function parseBody(event){const headers=event.headers||{},ct=(headers['content-type']||headers['Content-Type']||'').toLowerCase(),raw=event.body||'';if(ct.includes('application/json'))return JSON.parse(raw);const p=new URLSearchParams(raw);return{Ds_SignatureVersion:p.get('Ds_SignatureVersion'),Ds_MerchantParameters:p.get('Ds_MerchantParameters'),Ds_Signature:p.get('Ds_Signature')}}
async function postProcess(record,wasPaidBefore){if(record.status==='PAID'&&!wasPaidBefore&&!record.amountMismatch){try{await finalize.finalizePaid(record,{source:'redsys',sendCustomerEmail:true})}catch(err){console.error('[Redsys-notify] Finalización enterprise pendiente:',err.message)}}else if(record.status==='FAILED'&&!wasPaidBefore){await finalize.finalizeFailed(record).catch(err=>console.error('[Redsys-notify] No se liberó reserva:',err.message))}}
exports.handler=async function(event){if(event.httpMethod==='OPTIONS')return{statusCode:204,headers:HEADERS,body:''};if(event.httpMethod!=='POST')return{statusCode:405,headers:HEADERS,body:'Method Not Allowed'};const secretKey=process.env.REDSYS_SECRET_KEY;if(!secretKey)return{statusCode:500,headers:HEADERS,body:'Server misconfigured'};let data;try{data=parseBody(event)}catch{return{statusCode:400,headers:HEADERS,body:'Bad body'}}const{Ds_MerchantParameters,Ds_Signature}=data;if(!Ds_MerchantParameters||!Ds_Signature)return{statusCode:400,headers:HEADERS,body:'Missing parameters'};let params;try{params=JSON.parse(Buffer.from(Ds_MerchantParameters,'base64').toString('utf8'))}catch{return{statusCode:400,headers:HEADERS,body:'Invalid parameters'}}const order=params.Ds_Order||params.DS_ORDER;if(!order)return{statusCode:400,headers:HEADERS,body:'Missing order'};let computed;try{computed=toBase64Url(hmacBase64(Ds_MerchantParameters,deriveSigningKey(secretKey,order)))}catch{return{statusCode:500,headers:HEADERS,body:'Signature error'}}if(!safeEqual(computed,toBase64Url(Ds_Signature)))return{statusCode:403,headers:HEADERS,body:'Invalid signature'};const responseCode=parseInt(params.Ds_Response,10),authorised=Number.isInteger(responseCode)&&responseCode>=0&&responseCode<=99,result={order,amount:Number(params.Ds_Amount)/100,currency:params.Ds_Currency,responseCode:params.Ds_Response,authCode:params.Ds_AuthorisationCode||null,paymentType:params.Ds_PayMethod||null,status:authorised?'PAID':'FAILED',receivedAt:new Date().toISOString()};let record=result,wasPaidBefore=false;try{const s=await getStore();if(s){const previous=await s.get(order,{type:'json'}).catch(()=>null);wasPaidBefore=previous&&previous.status==='PAID';record={...(previous||{}),...result};const expectedCents=Math.round(Number(previous&&previous.amount)*100),chargedCents=parseInt(params.Ds_Amount,10);if(Number.isFinite(expectedCents)&&expectedCents!==chargedCents){record.amountMismatch={esperado:expectedCents/100,cobrado:chargedCents/100};console.error('[Redsys-notify] IMPORTE DISTINTO AL ESPERADO',order,expectedCents,chargedCents)}await s.setJSON(order,record)}}catch(err){console.error('[Redsys-notify] No se pudo persistir el pedido',order,err)}console.log('[Redsys-notify]',JSON.stringify({order,status:record.status,responseCode:record.responseCode}));if(authorised&&!wasPaidBefore){try{const{subject,html}=buildOrderEmail(record);await sendEmail({to:process.env.ORDER_NOTIFICATION_EMAIL,subject,html})}catch(err){console.error('[Redsys-notify] Fallo aviso de pedido:',err)}}await postProcess(record,wasPaidBefore);return{statusCode:200,headers:HEADERS,body:'OK'}};
exports._test={deriveSigningKey,hmacBase64,toBase64Url,safeEqual,parseBody,postProcess};
